# Scripts

Thin command entrypoints for this application root. HarmonyOS build and
packaging commands require `ohpm`/`hvigor` plus a signing profile and are
tracked as pending integration; this directory stays empty until those
toolchains are available in the workspace.
