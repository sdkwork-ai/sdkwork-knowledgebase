# SDKs

This application root does not own generated SDK workspaces. Generated app-api
and backend-api SDKs stay in the repository-level `sdks/` directory and are
consumed through the core package's injected ports
(`APP_SDK_INTEGRATION_SPEC.md`).
