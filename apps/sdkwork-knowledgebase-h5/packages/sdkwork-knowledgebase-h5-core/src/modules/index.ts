/**
 * Module registry barrel.
 *
 * Reusable module registration for the H5 root is assembled here; nothing is
 * exported yet because the knowledgebase capability ships as a single feature
 * package.
 */
export {};
