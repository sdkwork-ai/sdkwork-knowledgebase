/**
 * Authored locale fragment: knowledgebase search (zh-CN).
 *
 * Authority: `I18N_SPEC.md` section 6.1 —
 * `src/i18n/<locale>/<domain>/<capability>/<fragment>.ts`.
 * Key names stay identical across locales; only the copy differs.
 */
export const knowledgebaseMpSearchZhCn: Record<string, string> = {
  "title": "检索",
  "placeholder": "搜索知识库内容",
  "empty": "没有匹配结果"
};
