/**
 * Authored locale fragment: knowledgebase launch (en-US).
 *
 * Authority: `I18N_SPEC.md` section 6.1 —
 * `src/i18n/<locale>/<domain>/<capability>/<fragment>.ts`.
 * Key names stay identical across locales; only the copy differs.
 */
export const knowledgebaseMpLaunchEnUs: Record<string, string> = {
  "title": "Group Knowledge Base",
  "verifying": "Verifying launch ticket",
  "accepted": "Group knowledge base opened",
  "invalid": "Launch ticket invalid"
};
