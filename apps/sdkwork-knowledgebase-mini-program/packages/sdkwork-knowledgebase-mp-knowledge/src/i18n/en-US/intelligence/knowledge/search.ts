/**
 * Authored locale fragment: knowledgebase search (en-US).
 *
 * Authority: `I18N_SPEC.md` section 6.1 —
 * `src/i18n/<locale>/<domain>/<capability>/<fragment>.ts`.
 * Key names stay identical across locales; only the copy differs.
 */
export const knowledgebaseMpSearchEnUs: Record<string, string> = {
  "title": "Search",
  "placeholder": "Search knowledge base content",
  "empty": "No matching result"
};
