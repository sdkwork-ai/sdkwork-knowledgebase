# skills/

Application-local agent skills for `apps/sdkwork-knowledgebase-mini-program`.

Each skill is a directory containing `SKILL.md`; local skills narrow the
repository standards for mini-program-only work and must not restate global
specs from `../../../sdkwork-specs/`.
